# Biweekly Research Stack Review — 2026-08-01

## MCP Health

| Connector | Probe | Status | Notes |
|---|---|---|---|
| **Bigdata.com** | `bigdata_search text="test"` | ✓ Healthy | Returned 3 chunks; token types: corporate_comms (191), transcripts (209), web (160) |
| **FMP** | `quote symbol=AAPL` | ✓ Healthy | AAPL $308.91, mktcap $4.54T, vol 127M — full data returned |
| **Quartr** | `search_companies query="Apple"` | ✗ Pro-gated | `subscription_required` error — Pro plan needed, not just degraded |
| **IBKR** | `get_account_summary` | ✓ Healthy | Net liq $82,768.74, buying power $253,606.94, positions $77,468 |

## Bigdata.com Trial Status

**Verdict: Expired.**

Trial started 2026-05-26; SOP states ~13-day trial window → should have expired ~2026-06-08. Today is day ~67. The "test" search returns generic public-web and Quartr presentation content — consistent with free-tier behavior. No evidence of premium research reports or gated broker content in the result set. Main stack already falls back to EdgarTools + WebFetch per SOP.

## URL Spot Check

Three URLs tested spanning price, insider tracking, and investor letters categories.

| Category | URL | Result |
|---|---|---|
| Price / statistics | `stockanalysis.com/stocks/aapl/statistics/` | 403 — anti-bot block; URL valid (not 404) |
| Insider tracking | `openinsider.com/screener?s=AAPL` | 403 — anti-bot block; URL valid (not 404) |
| Investor letters | `berkshirehathaway.com/letters/letters.html` | 403 — anti-bot block; URL valid (not 404) |

**Note:** All three returned HTTP 403 (scraper protection), not 404. The URLs themselves are not broken — these sites block headless/proxy traffic. This is a remote-execution environment limitation, not URL rot. In interactive teardown sessions the user runs WebFetch directly and results may differ. IBKR MCP is the primary price source anyway.

## Synthesis & Recommendation

**No changes needed.**

Stack status: IBKR (primary price + positions) and FMP are both healthy. EdgarTools (SEC filings) not probed this cycle but has no subscription dependency. Bigdata.com has been on free tier for ~54 days with no new capability unlocked. Quartr has been Pro-gated since initial setup — unchanged.

Per CLAUDE.md principle ("AI 时代要持续学习 = 溶解闸门的万能借口"): no tool addition is warranted. No blocked decision in the past two weeks has been attributed to a missing source. The stack at $0/month continues to cover all teardown steps via IBKR + EdgarTools + WebFetch.

**Flags (no action):**
- Quartr remains Pro-gated — only worth revisiting if a specific transcript was needed and was unavailable via Motley Fool / AlphaStreet WebFetch.
- WebFetch 403 rate on spot-checked sites is worth noting at the next quarterly deep review (Q3 end) — if interactive sessions are also failing these sites, the backup WebFetch fallback for price data is impaired.
